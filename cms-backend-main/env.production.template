# ============================================
# CMS Backend - Production Environment Variables
# For Verpex VPS Deployment
# ============================================
# Copy this file to .env.production and fill in your actual values

# ============================================
# Node Environment
# ============================================
NODE_ENV=production

# ============================================
# Server Configuration
# ============================================
# Backend server port (adjust if needed for your VPS setup)
BACKEND_PORT=8000

# ============================================
# Database Configuration
# ============================================
# PostgreSQL connection string
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?sslmode=require
# Example for remote PostgreSQL:
# DATABASE_URL=postgresql://username:password@your-db-host:5432/cms_db?sslmode=require
# Example for local PostgreSQL on VPS:
# DATABASE_URL=postgresql://username:password@localhost:5432/cms_db
DATABASE_URL=postgresql://username:password@localhost:5432/cms_db

# ============================================
# JWT Authentication
# ============================================
# CRITICAL: Generate strong, random secrets for production
# Use a secure random string generator (at least 32 characters)
# You can generate one using: openssl rand -base64 32
JWT_SECRET=your-super-secure-jwt-secret-key-minimum-32-characters-long

# Optional: Separate refresh token secret (if not provided, JWT_SECRET will be used)
JWT_REFRESH_SECRET=your-super-secure-refresh-token-secret-key-minimum-32-characters-long

# JWT token expiration times
# Access token expiration (default: 24h)
JWT_EXPIRES_IN=24h

# Refresh token expiration (default: 7d)
JWT_REFRESH_EXPIRES_IN=7d

# ============================================
# CORS Configuration
# ============================================
# Allowed origins for CORS (comma-separated)
# Replace with your actual frontend domain(s)
# Example: https://yourdomain.com,https://www.yourdomain.com
# For Verpex VPS, use your domain name or IP
ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# ============================================
# Security & Rate Limiting
# ============================================
# Rate limiting is automatically configured based on NODE_ENV
# Production: 100 requests per 15 minutes per IP
# Development: 1000 requests per 15 minutes per IP

# ============================================
# Optional: Email Service (if implemented)
# ============================================
# Uncomment and configure if you plan to use email notifications
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-email-password
# SMTP_FROM=noreply@yourdomain.com

# ============================================
# Optional: SMS Service (if implemented)
# ============================================
# Uncomment and configure if you plan to use SMS notifications
# Example for Twilio:
# TWILIO_ACCOUNT_SID=your-twilio-account-sid
# TWILIO_AUTH_TOKEN=your-twilio-auth-token
# TWILIO_PHONE_NUMBER=+1234567890

# ============================================
# Optional: Logging
# ============================================
# Log level (optional, defaults to console logging)
# LOG_LEVEL=info

# ============================================
# Notes for Verpex VPS Deployment
# ============================================
# 1. Make sure PostgreSQL is installed and running on your VPS
# 2. Create a database user and database for the CMS
# 3. Update DATABASE_URL with your actual database credentials
# 4. Generate strong JWT secrets using: openssl rand -base64 32
# 5. Update ALLOWED_ORIGINS with your actual frontend domain
# 6. Ensure the backend port (8000) is open in your firewall
# 7. Consider using a reverse proxy (nginx) for production
# 8. Use PM2 or similar process manager to keep the server running
# 9. Set up SSL certificates for HTTPS (Let's Encrypt recommended)
# 10. Keep this file secure and never commit it to version control

